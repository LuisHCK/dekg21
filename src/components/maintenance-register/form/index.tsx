import React from 'react'
import { Row } from 'react-bootstrap'
import { TFormSet } from 'types/machinery'

type TMaintenanceRegisterProps = {
    formset: TFormSet
}

const MaintenanceRegisterForm = ({ formset }: TMaintenanceRegisterProps): React.ReactElement => {
    return <div></div>
}

export default MaintenanceRegisterForm
